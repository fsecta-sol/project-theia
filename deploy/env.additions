# Keys Theia needs in ~/.hermes/.env (NAMES only — real values copied from local .secret
# by deploy.sh, never printed). GoPlus is optional (keyless free tier works).
HELIUS_API_KEY
BIRDEYE_API_KEY
# optional:
# GOPLUS_APP_KEY
# GOPLUS_APP_SECRET
